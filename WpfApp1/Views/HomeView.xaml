<views:MvxWpfView x:Class="WpfApp1.Views.HomeView"
             xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             xmlns:vms="clr-namespace:PortClosedEmailer.Core.ViewModels;assembly=PortClosedEmailer.Core"
             d:DataContext="{d:DesignInstance vms:HomeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <local:HostsDataGrid />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBox Text="{Binding NewHostName, Mode=TwoWay}" VerticalAlignment="Center"
                     Width="300" Padding="10 5 5 5" VerticalContentAlignment="Center" />
            <Button Content="add host" Command="{Binding AddHostCmd}" 
                    HorizontalAlignment="Right" Padding="10 5 10 5" Margin="10 10 10 10" />
        </StackPanel>
    </StackPanel>
</views:MvxWpfView>
